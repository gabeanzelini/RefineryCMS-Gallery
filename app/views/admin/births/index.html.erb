<div id='actions'>
  <ul>
    <li>
      <%= render :partial => "/shared/admin/search", :locals => {:url => admin_births_url} %>
    </li>
    <li>
      <%= link_to t('.create'), new_admin_birth_url, :class => "add_icon" %>
    </li>
  </ul>
</div>
<div id='records'>
  <% if searching? %>
    <h2><%= t('shared.admin.search.results_for', :query => params[:search]) %></h2>
  <% end %>
  <% if @births.any? %>
    <%= will_paginate @births %>
    <ul>
      <%= render :partial => "birth", :collection => @births %>
    </ul>
    <%= will_paginate @births %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong><%= t('.no_items_yet') %></strong>
      <% else %>
        <%= t('shared.admin.search.no_results') %>
      <% end %>
    </p>
  <% end %>
</div>
